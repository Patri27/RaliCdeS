export interface Event {
  name: string;
  description: string;
  date: Date;
  location: string;
}

export interface EventRequest {
  name: string;
  description: string;
  date: Date;
  location: string;
}
